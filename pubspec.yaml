name: flutter_multithreading_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  provider: ^6.0.5
  stream_channel: ^2.1.1
  uuid: ^3.0.7

dev_dependencies:
  lint: ^2.0.1

flutter:
  uses-material-design: true
